# Copy this file to ../secrets/hyperliquid.env and fill values locally only if execution tooling is added.
# The current collectors use public info endpoints and do not require a private key.

HYPERLIQUID_API_WALLET=
HYPERLIQUID_PRIVATE_KEY=
